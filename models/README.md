# Database Models - EventHub

This directory contains the database entities and schemas for EventHub:

- **`schema.prisma`**: The single source of truth database model file defining the `Club`, `Event`, `User`, `RSVP`, and `AcademicCalendar` schemas.

*Note: For migration instructions, please refer to the main [README.md](../README.md) at the root of the repository.*
